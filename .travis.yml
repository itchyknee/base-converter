language: java

addons:
  sonarqube:
    organization: "itchyknee-github"
    token:
      secure: $SONAR_TOKEN

jdk:
  - oraclejdk8
  
script:
 - mvn clean install sonar:sonar

cache:
  directories:
  - "$HOME/.m2/repository"
  - "$HOME/.sonar/cache"
